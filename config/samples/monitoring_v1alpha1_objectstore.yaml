apiVersion: monitoring.banzaicloud.io/v1alpha1
kind: ObjectStore
metadata:
  name: objectstore-sample
spec:
  config:
    value: my-objectstore-secret
  compactor:
    enable: true
  bucketWeb:
    enable: true
